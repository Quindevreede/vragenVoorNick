Les 4 Scope

- two :

Waarom kan ik hier niet (int)(Math.random() * 101); gebruiken?

Ik heb dit geprobeerd om som van getallen tot 10 te krijgen;

            for (int i = 0; i < 10; i++) {
            Random random = new Random((int)(Math.random() * 11));
            totalsum = totalsum + random.nextInt();